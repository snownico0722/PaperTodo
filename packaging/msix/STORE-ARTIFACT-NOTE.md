# Store artifact note

The Microsoft Store artifact must be built after the hardened portable-data import validation introduced for the 3.x Store packaging path. The Store package uses the official Partner Center identity and remains unsigned for Partner Center submission.
