{
  "kind": "native",
  "id": "tools.codex-cli-bridge.native",
  "name": "Codex CLI Bridge",
  "description": "Send PaperTodo todos and paper content directly to the local Codex CLI.",
  "version": "1.2.0",
  "apiVersion": "2.1",
  "stateVersion": 1,
  "entry": "PaperTodo.Plugin.CodexCliBridge.dll",
  "maxPaperInstances": 1,
  "capabilities": [
    "runtime"
  ],
  "permissions": [
    "papers.read",
    "papers.observe",
    "todos.read",
    "todos.observe",
    "notes.read"
  ],
  "settings": [
    {
      "id": "autoStart",
      "type": "boolean",
      "name": "Enable on startup",
      "description": "Automatically create a collapsed Codex CLI plugin paper to keep the global todo and top-bar actions available.",
      "default": true,
      "quick": true
    },
    {
      "id": "backgroundExecution",
      "type": "boolean",
      "name": "Run in background",
      "description": "Hide the terminal window and run Codex silently. Off by default; when off, Codex always opens in the foreground.",
      "default": false,
      "quick": true
    },
    {
      "id": "model",
      "type": "string",
      "name": "Model",
      "description": "Model ID passed to Codex CLI. Defaults to gpt-5.6-sol; leave empty to keep the Codex CLI model setting.",
      "default": "gpt-5.6-sol",
      "maxLength": 128,
      "quick": true
    },
    {
      "id": "reasoningEffort",
      "type": "string",
      "name": "Reasoning",
      "description": "Value passed to model_reasoning_effort. Defaults to xhigh; leave empty to keep the Codex CLI setting.",
      "default": "xhigh",
      "maxLength": 32,
      "quick": true
    },
    {
      "id": "codexPath",
      "type": "string",
      "name": "Codex CLI command",
      "description": "Uses codex from PATH by default. You can also enter codex.cmd or an absolute path.",
      "default": "codex",
      "maxLength": 512
    },
    {
      "id": "workingDirectory",
      "type": "string",
      "name": "Default working directory",
      "description": "Used when no local path is linked. Leave empty to use the user profile directory; environment variables such as %USERPROFILE% are supported.",
      "default": "",
      "maxLength": 1024
    }
  ],
  "startupPaper": {
    "enabledSetting": "autoStart",
    "instanceKey": "main",
    "presentation": "capsule",
    "title": "Codex CLI"
  },
  "locales": {
    "zh": {
      "description": "从 PaperTodo 待办项和纸片顶栏把内容直接发送给本机 Codex CLI。",
      "settings": {
        "autoStart": {
          "name": "启动后自动启用",
          "description": "自动创建一张折叠的 Codex CLI 插件纸片，用于保持全局待办按钮和顶栏按钮可用。"
        },
        "backgroundExecution": {
          "name": "后台执行",
          "description": "隐藏终端窗口并静默运行 Codex。默认关闭；关闭时 Codex 始终以前台窗口运行。"
        },
        "model": {
          "name": "模型",
          "description": "传给 Codex CLI 的模型 ID。默认 gpt-5.6-sol；留空则不覆盖 Codex 自己的模型配置。"
        },
        "reasoningEffort": {
          "name": "推理",
          "description": "传给 model_reasoning_effort。默认 xhigh（极高）；留空则不覆盖 Codex 自己的推理配置。"
        },
        "codexPath": {
          "name": "Codex CLI 命令",
          "description": "默认使用 PATH 中的 codex。也可以填写 codex.cmd 或绝对路径。"
        },
        "workingDirectory": {
          "name": "默认工作目录",
          "description": "没有绑定本地路径时使用。留空则使用用户目录；支持 %USERPROFILE% 等环境变量。"
        }
      },
      "startupPaper": {
        "title": "Codex CLI"
      }
    }
  }
}
