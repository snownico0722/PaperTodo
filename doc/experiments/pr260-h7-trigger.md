Temporary trigger for the H7 constrained-preview recovery workflow. Remove after the focused experiment finishes.

Retry after correcting the capacity-release, requested-size and deferred-content patch anchors.

R5 retry: select the primary request constructor exactly, resolve the private static QueueKey overload explicitly, recreate the real controller layout collections required by the uninitialized test fixture, verify controller-rejection rollback after Host replacement, and report the exact remaining patch anchor if fixture finalization still fails before running frozen #260 baseline and full native-input.
