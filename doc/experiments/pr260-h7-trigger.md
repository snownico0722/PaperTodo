Temporary trigger for the H7 constrained-preview recovery workflow. Remove after the focused experiment finishes.

Retry after correcting the capacity-release, requested-size and deferred-content patch anchors.

R5 retry: use the simplified candidate-only rollback insertion, restore the partial controller's required layout collections, verify controller-rejection rollback after Host replacement, then run the frozen #260 baseline and full native-input before promotion.
